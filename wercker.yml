box: rocker/hadleyverse
build:
    steps:
        - jimhester/r-dependencies:
            cwd: prtools
        - inbobmk/r-check:
            cwd: prtools
        - inbobmk/r-coverage:
            cwd: prtools
    after-steps:
        - script:
            code: |
                Rscript -e "library(lintr); lint_package('mbtools', linters=
                with_defaults(trailing_blank_lines_linter=NULL))"
